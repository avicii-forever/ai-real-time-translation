# 实时视频翻译 · 项目评估报告

> 评估日期:2026-08-17
> 评估方式:直接 SSH 到 OpenLibing 的 Ascend 910B 节点,实测已部署的 llama.cpp-omni + MiniCPM-o 4.5

## 一句话结论

部署是**真实且大部分能跑**的:框架在昇腾 910B 上能加载、LLM 文本生成够快(约 59 tok/s)。
但你选的"**语音配音**"路线,当前被一个**声码器 crash bug** 卡死;文本翻译的输入链路也还没接通。
**不是"昇腾跑不动",是"这个 fork 在昇腾上有两个还没修好的坑"。**

---

## 1. 实测部署栈

| 项 | 实测值 |
|---|---|
| 硬件 | 单卡 Ascend 910B3,64GB HBM(节点 `openlibing-omni`,<节点IP>) |
| 容器 | Docker(主机名是容器 ID),openEuler 22.03 SP4,aarch64 |
| CANN | 9.1.0-beta.3,driver 25.2.0 |
| 推理框架 | `tc-mb/llama.cpp-omni` fork(走 gh-proxy 拉取),CANN 后端已编译 |
| 权重 | MiniCPM-o 4.5 **F16 GGUF**:主 LLM 16GB + 音频 630M + 视觉 1.1G + TTS 1.1G + 声码器 token2wav ~880M ≈ **19.7GB** |
| 二进制 | `llama-omni-server` / `llama-omni-cli`,`libggml-cann.so`(昇腾后端)在 |
| 启动脚本 | `/workspace/llama.cpp-omni/start_server.sh`,端口 28099 |

## 2. 实测结果

| 环节 | 结果 | 耗时 |
|---|---|---|
| 服务启动 | `/health` 返回 `{"engine":"comni","status":"ok"}` | 3s |
| 全管线加载 `omni_init` | **成功**,CANN0(NPU)后端激活 | **6.5s** |
| 文本 prefill | 成功 | 0.52s |
| LLM 文本解码 | 成功,流式 SSE 出字,~180 token | 3.03s ≈ **~59 tok/s** |
| TTS 语音合成(声码器) | **crash** | — |

LLM 解码速度 ~59 tok/s 对 9B 模型在 910B 上是**合格偏优**的,一行字幕 10-20 token,解码 <1s,实时性够用。

## 3. 两个 blocker(实测发现)

### Blocker 1:TTS 声码器在昇腾上 crash —— 直接卡死"语音配音"

第一次 decode(use_tts=true)时,LLM 出文本后走到 TTS/Token2Wav 声码器,**进程 abort 崩溃**:

```
ggml-cann.cpp:70: CANN error
rtMemcpyAsync execution failed, the context is a null pointer.
current device: -1, in ggml_backend_cann_set_tensor_async
```

崩溃回溯:`Token2WavSession::feed_window → Token2Mel::infer_one_chunk → flowGGUFModelRunner → ggml_backend_cann_set_tensor_async → ggml_abort`,发生在独立的 `t2w_thread_func_cpp` 线程。

**根因**:CANN 上下文在主线程初始化,却在 T2W 工作线程里用,设备上下文没传过去(device=-1)。典型 CANN 多线程 bug。LLM/视觉/音频走 CANN0 都正常,唯独声码器这个 flow 模型崩。

### Blocker 2:文本输入没喂进模型 —— 模型在"幻觉"

prefill 传 `text:"今天天气很好,我们出去走走吧。"`,即便设了翻译系统提示词,模型也完全忽略输入,输出了一段关于"去大峡谷旅行"的英文——**说明 user 文本没有真正进入 LLM 的输入**,模型在空输入下自由发挥。

需要排查:`stream_prefill` 的 `text` 参数是格式不对(需要套 chat template)还是 fork 的 bug。

## 4. 对产品目标的影响

| 产品形态 | 现状 |
|---|---|
| 双语字幕(音频→文本翻译) | LLM 生成能跑,但**翻译输入没接通**,且真实翻译质量尚未验证 |
| 语音配音(音频→语音 S2ST) | **声码器 crash,当前跑不通** |

两个 bug 都是"能修"级别的(不是昇腾根本做不了),但都需要动 C++ 源码 + 重编译。

## 5. 实现路线(重估后)

- **路线 A:端到端 S2ST(配音)**
  - 前提是修好 Blocker 1(TTS crash)。修法大概率是把 CANN device context 绑定到 T2W 线程,或等 tc-mb fork 上游修复。
  - 工程量大(改 C++ / 找上游 issue / 重编译)。

- **路线 B:字幕(文本翻译)优先**
  - 先修 Blocker 2(文本输入),用文本验证 MiniCPM-o 4.5 的翻译质量。
  - 如果翻译质量过关,字幕 demo 最快能出来。

- **路线 C:级联绕过 crash(配音的权宜)**
  - LLM 只出翻译文本(use_tts=false,已确认不崩),**换一个独立 TTS**(如 ChatTTS / CosyVoice / 华为的 TTS)做语音合成,绕开 omni 自带的 Token2Wav。
  - 损失全双工和音色一致性,但能最快做出"配音"演示。

## 6. 下一步建议

1. **先验证翻译质量**(最高优先级,决定整个产品成不成立):修 Blocker 2,用文本让模型真正翻译一句中→英,看质量能不能用。
2. 如果翻译质量 OK → 走路线 B(字幕)做首版 demo,再评估路线 C(级联配音)或 A(修 TTS)。
3. 修 Blocker 1 前,先搜 tc-mb/llama.cpp-omni 的 issue 有没有人报过昇腾 T2W 崩溃,别自己从头啃。

## 7. 待你决策

- 首版到底做**字幕**还是坚持**配音**?(配音当前有 crash,代价高)
- 两个 bug 要不要我继续往下修?(改 C++ 源码 + 重编译,属于实现阶段,超出纯评估)

---

## 8. 字幕路线验证(2026-08-17 更新,已定:做字幕)

进一步实测,**音频→翻译文本这条字幕核心链路在昇腾上端到端跑通**:

| 环节 | 结果 |
|---|---|
| 音频 APM 编码 | ✅ 每个 1s chunk ~0.08s |
| 音频 embedding → LLM | ✅ 正确进入上下文(n_pos=10) |
| LLM 解码 | ✅ ~46-59 tok/s,短输出正常 |
| 翻译输出 | ✅ 能出英文,且不漫谈(需真实内容验证质量) |

**关键发现(提示词格式)**:
- omni 的提示词必须带音频锚点 token,格式:
  - `voice_clone_prompt` = `<|im_start|>system\n{系统指令}\n<|audio_start|>`
  - `assistant_prompt` = `<|audio_end|>{任务指令}<|im_end|>\n<|im_start|>user\n`
- 传纯文本会弄丢 `<|audio_start|>`/`<|audio_end|>`,导致音频 embedding 无处安放、decode 空返回。
- 默认提示词是"语音克隆+闲聊",会让模型狂生成 4000+ token 不停止;翻译提示词能压成短输出。

**Blocker 2 更正**:文本 `text` 字段在 audio 模式下不适用(音频走 APM),之前的"幻觉"是纯文本模式的问题,不影响字幕路线。

**剩余待办**:用真实的中文语音验证翻译质量(现成测试音频内容未知,输出 "Story." 无法判断对错)。

## 9. 字幕落地计划

首版 demo 建议分两步,先离线后实时:

1. **离线字幕生成器**(最快验证):音频/视频文件 → 切 1s chunk → omni 翻译 → 输出 `.srt` 字幕文件(带时间戳)。验证翻译质量、产出可交付物。
2. **实时字幕**:麦克风/直播流 → 流式切 chunk → 流式翻译 → 叠加字幕显示。处理延迟和缓冲。
