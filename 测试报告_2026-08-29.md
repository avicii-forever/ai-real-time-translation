# AI 实时翻译 · 全功能测试报告

> 测试日期:2026-08-29
> 测试人:Claude(自动化回归)

---

## 一、测试环境

| 项 | 值 |
|---|---|
| 后端实例 | `DevEnv_775164`(<节点IP>) |
| 推理硬件 | Ascend **910B4**,32GB HBM,NPU 逻辑 ID 0 |
| 推理框架 | tc-mb/llama.cpp-omni @ `6e9ae1a` + 本地补丁(含 emb_cache 复用) |
| 模型 | MiniCPM-o-4_5 F16(LLM 16.4GB + audio/vision/tts/token2wav) |
| 权重路径 | `/workspace/shared_assets/models/OpenBMB/MiniCPM-o-4_5-gguf/`(软链到 `/workspace/MiniCPM-o-4_5-gguf`) |
| 客户端 | Windows 11 + Python 3.13 + tkinter |
| 产物 | `dist/AI实时翻译.exe`(38.4MB,11:21 打包) |

---

## 二、测试结果总览

| # | 功能 | 结果 | 关键数据 |
|---|---|---|---|
| 1 | 后端健康检查 | ✅ PASS | `/health` → `{"engine":"comni","status":"ok"}` |
| 2 | 文本流式翻译(WS duplex) | ✅ PASS | 60s 英文 → 10 条中文字幕,首字延迟 12.1s |
| 3 | 滚动 session(长会话回收) | ✅ PASS | 30s 触发回收,回收后正常续译 |
| 4 | emb_cache 复用(回收加速) | ✅ PASS | 回收时 `encoder_thread created` 仅 **0.2s**,不再重读权重 |
| 5 | 有声翻译(TTS 配音) | ✅ PASS | 60s 英文 → 34s 中文配音(压缩比 0.57x),38 包 |
| 6 | 字幕弹窗(GUI) | ✅ PASS | open/set_text/锁定/关闭 冒烟全通过 |
| 7 | exe 打包 | ✅ PASS | 38.4MB,含字幕弹窗模块 |

**结论:7 项核心功能全部通过。**

---

## 三、分项详情

### 1. 后端健康检查 ✅

```
GET http://127.0.0.1:28099/health
→ {"engine":"comni","status":"ok"}
```

NPU `Health = OK`,后端进程存活,模型常驻(shared_octx 复用)。

### 2. 文本流式翻译(WS duplex)✅

测试命令:
```
python cli_test_video.py --wav seg_000500.wav --t 60 --mode continuous
```

| 指标 | 值 |
|---|---|
| 字幕数 | 10 条 |
| session.init | 66.2s(use_tts 切换冷加载,一次性) |
| 首字延迟(推流起算) | 12.1s |

译文示例(前几条):
```
[23.4s] 我们应该关注的是时钟速度...
[31.5s] 长一段时间内表现不错直到200千年代...
```

### 3. 滚动 session ✅ / 4. emb_cache 复用 ✅

测试命令(60s 片段 + `--max-session 30`,强制 30s 回收一次):
```
python cli_test_video.py --wav seg_000500.wav --t 60 --max-session 30
```

- 30s 时触发「会话已跑 30.0s,滚动回收」→「WS 会话已建立」,回收后继续翻译。
- 服务端日志关键时序(emb_cache 复用生效):

```
05:03:19.692 Duplex: threads stopped (pipeline kept for reuse)  ← 回收(保留 pipeline)
05:03:19.909 Duplex: encoder_thread created                      ← 0.2s 后重建线程
```

- `emb_cache initialized` 只出现 5 次(对应 5 次冷加载),**回收时不再重读 19.7GB 权重**。
  对比修复前:每次回收重读权重约 65s,现在约 0.2s。

### 5. 有声翻译(TTS 配音)✅

测试命令:
```
python cli_test_video.py --wav seg_000500.wav --t 60 --dub-save-only
```

| 指标 | 值 |
|---|---|
| 配音音轨 | 34.0s(原声 60s) |
| 压缩比 | 0.57x(中文更紧凑,边翻边播不掉队) |
| 配音包数 | 38 段 |
| session.init | 3.0s(热复用) |

### 6. 字幕弹窗(GUI)✅

冒烟测试:open → set_text → set_locked(点击穿透)→ 解锁 → close,全部通过。

功能:
- 无边框、半透明(78%)、永远置顶;
- 未锁定时可拖动;
- 锁定时点击穿透(不影响下一层界面点击),靠主界面勾选框解锁;
- 实时显示当前译文(与主界面「译文」区同步)。

### 7. exe 打包 ✅

- 产物 `dist/AI实时翻译.exe`,38.4MB,2026-08-29 11:21 打包;
- 已确认包含字幕弹窗模块(`subtitle_overlay`)、配音缓冲修复、滚动 session、最新 config(新 IP / `MAX_SESSION_SECONDS=110` / 超时 320s)。

---

## 四、已知问题(未解决)

| 问题 | 状态 | 说明 |
|---|---|---|
| 英文漂移 | ⚠️ 未解决 | 翻译中间歇性转成英文转写(实测 35s 起就有),已确认**与会话时长无关**(80s 档 26s 就出现),是「转写 vs 翻译」切换的翻译质量问题,方向在提示词 / `listen_prob_scale` / 采样参数 |

---

## 五、备注

- 后端 use_tts 切换会触发冷加载(shared_octx 要求 duplex/use_tts 都匹配),首次连接约 1-7 分钟,属正常现象。
- loopback 回授:采集源选「系统声音」且配音同扬声器播放时会回授,建议「麦克风 + 耳机」。
- 本次测试覆盖核心链路;断线重连 + 看门狗为早期已验证功能,本轮未重复测试。
