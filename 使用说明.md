# AI 实时翻译 · 语音转语音(V2V)使用说明

> 版本:2026-08-26 · 对应后端:昇腾 910B 节点 `<SSH别名>` · 模型:MiniCPM-o 4.5

## 一、这是什么

把**中文语音**实时翻译成**英文**:采集系统声音或麦克风 → 昇腾 910B 上的 MiniCPM-o 4.5 翻译 → **边说边出英文译文**(可选语音输出)。

**核心特性(全双工)**:不用等整句话说完整,每说完 1.5 秒,译文就开始滚动 —— 说完即基本完整。

---

## 二、系统要求

| 项 | 要求 |
|---|---|
| 本机 | Windows,Python 3.10+ |
| 网络 | 能 SSH 到 OpenLibing 网关(<网关IP>:2222) |
| 后端 | 昇腾 910B 节点(已部署,见 §六) |
| 音频 | 有声卡(系统声音/麦克风),无需额外设备 |

---

## 三、首次安装

### 1. 安装 Python 依赖

```bash
pip install soundcard paramiko numpy websocket-client
# tkinter 随 Python 自带;winsound 随 Windows 自带
```

### 2. 确认 SSH 配置

`C:\Users\chw\.ssh\config` 里需有:
- `<SSH别名>`(后端推理节点)
- `jump-1784208342087`(网关,ProxyJump 用)

```bash
ssh <SSH别名> "echo OK"   # 应输出 OK
```

### 3. 确认后端服务在跑(见 §六)

---

## 四、日常使用

### 启动(两步)

```bash
# ① 建 SSH 隧道(把后端 28099 暴露到本机;保持窗口开着)
ssh -N -L 28099:127.0.0.1:28099 <SSH别名>

# ② 另开一个终端,启动客户端
cd client
python main.py
```

> 也可用一键脚本:`powershell -ExecutionPolicy Bypass -File scripts\connect.ps1`(只建隧道)。

### 界面操作

1. **输入源**:选「系统声音」(翻译电脑播放的音频)或「麦克风」(翻译你说话)
2. **模式**:选 `duplex`(默认,边说边翻)或 `http`(整句翻译+语音,旧方案)
3. **语音输出**:勾选 = 播放英文 TTS 语音;不勾 = 只显示文本
4. 点 **「开始」** → 开始说话/播放 → 译文实时滚动
5. 点 **「停止」** 结束

### 无界面测试

```bash
cd client
python cli_test_ws.py --once 60    # duplex 流式,采 60 秒
python cli_test.py --once 60       # http 整句,采 60 秒
```

---

## 五、配置说明(`client/config.py`)

| 配置项 | 默认 | 说明 |
|---|---|---|
| `BACKEND_HOST/PORT` | 127.0.0.1:28099 | 后端地址(经隧道) |
| `SAMPLE_RATE` | 24000 | 采集采样率,**后端硬约束,勿改** |
| `SLICE_SECONDS` | 1.5 | 每帧切片时长(流式粒度) |
| `SILENCE_END_SECONDS` | 1.5 | 静音多久判定一句话结束 |
| `MAX_UTTERANCE_SECONDS` | 8 | 单句最长(超长自动分段) |
| `VAD_THRESHOLD` | 0.012 | VAD 能量阈值(自适应) |
| `USE_TTS / TTS_PLAY` | True / True | 是否合成并播放英文语音(http 模式用) |
| `VOICE_CLONE_PROMPT` | 中→英 | 翻译指令。改语言对(如英→中)就改这里 |
| `SSH_CONFIG` | 网关+节点 | 嵌套 SSH 凭据 |

**改语言对示例**(英→中):把 `VOICE_CLONE_PROMPT` 改成
`请把用户输入的英文语音逐句翻译成中文,只输出中文译文本身`。

---

## 六、后端(昇腾节点)运维

### 节点信息

- 别名:`<SSH别名>`(<节点IP>,经网关)
- 服务:`llama-omni-server`,端口 **28099**,单卡 910B3
- 部署目录:`/workspace/llama.cpp-omni/`,权重 `/workspace/MiniCPM-o-4_5-gguf/`
- 日志:`server.log`(同目录)

### 检查 / 重启

```bash
# 检查服务是否健康(本机经隧道)
curl http://127.0.0.1:28099/health     # 期望 {"engine":"comni","status":"ok"}

# 远端重启服务(释放模型 19.7GB 显存 + 恢复健康)
ssh <SSH别名> "bash /workspace/llama.cpp-omni/restart_backend.sh"
# 或本机:ssh <SSH别名> "bash -s" < scripts/restart_backend.sh
```

重启后需**重新建隧道**(§四)。

### 资源释放(翻译完成后)

- **模型(19.7GB)常驻**是设计:翻译后保留在显存,下次翻译免加载(快)。**这是有意的,不算泄漏**。
- 需要释放时:**重启服务**即可(上面的 restart_backend.sh)。已验证:重启后 NPU 显存从 17.6GB 降到 ~105MB。
- 重启脚本已优化:`pkill -x` 精确杀服务、`setsid` 脱离会话启动 —— 不会再出现"pkill 把 SSH 也杀了导致服务起不来"的问题。

### 服务参数(`start_server.sh`)

```
llama-omni-server -m <F16.gguf> --host 0.0.0.0 --port 28099 -n 256 -c 4096 -ngl 99
```
- `-n 256` 生成长度、`-c 4096` 上下文、`-ngl 99` 全量上卡

---

## 七、已知限制

| 限制 | 说明 |
|---|---|
| **采样率必须 24kHz** | 16kHz 输入翻不对(APM 编码错乱) |
| **duplex 需后端 patch** | 3 个本地改动(24k / 提示词注入 / hard-listen),已应用并重编译,勿覆盖 |
| **单句 ≤8s** | 超长自动分段;duplex 下每帧 1.5s 流式 |
| **多轮 TTS(http 模式)** | 同 session 第二轮 TTS 不产出(后端 bug);duplex 模式无此问题 |
| **连续快速说话** | VAD 静音 1.5s 才切句,语速过快可能粘连 |
| **译文小瑕疵** | 偶有 "flight" 类词义偏差(模型行为,非链路问题) |

---

## 八、故障排查

| 现象 | 排查 |
|---|---|
| `后端连接失败` | 隧道没建/断了 → 重跑 §四①,`curl /health` 确认 |
| 译文一直空 | ①后端重启过 → 重建隧道 ②没检测到语音 → 调大音量/检查输入源 |
| 译文不滚动 | 模式选成了 `http`(整句) → 改 `duplex` |
| GUI 无声音输出 | 语音输出未勾选,或系统静音;http 模式才有 TTS 语音 |
| 卡在"采集中" | 麦克风/loopback 权限被占用(其他程序独占声卡) |
| 服务 Health 失败 | 远端重启服务(§六)+ 重建隧道 |

---

## 九、项目结构与文件

```
E:\ai-real-time-translation\
├─ client\                # 客户端(主目录)
│  ├─ main.py             # GUI 入口(duplex/http 模式切换)
│  ├─ pipeline_ws.py      # WS 全双工流式翻译(默认)
│  ├─ pipeline.py         # HTTP 整句翻译(旧)
│  ├─ api\
│  │  ├─ ws_duplex_client.py   # WS full_duplex 协议封装
│  │  └─ omni_client.py        # HTTP REST 封装
│  ├─ audio\              # 采集 / VAD / 切片 / 播放
│  ├─ transport\          # 嵌套 SSH/SFTP
│  ├─ config.py           # 全部配置
│  ├─ cli_test_ws.py      # 无界面 duplex 测试
│  └─ cli_test.py         # 无界面 http 测试
├─ scripts\
│  ├─ connect.ps1         # 一键建隧道
│  └─ restart_backend.sh  # 远端重启服务
├─ probes\                # 验证/基准脚本(开发用)
├─ patches\               # 后端 patch 备份
├─ docs\                  # 部署记录
└─ PROJECT_STATUS.md      # 项目状态总览
```

---

## 十、后端 patch 清单(勿丢)

3 个本地改动(在 `/workspace/llama.cpp-omni`,`git status` 可见),是 duplex 翻译的关键:

1. `tools/server/ws_handler.cpp`:`write_audio_wav` 采样率 **16000→24000**
2. `tools/server/ws_handler.cpp`:duplex 下 `system_prompt` 注入 **voice_clone 前缀**(翻译指令进系统提示词)
3. `tools/omni/omni.cpp`:
   - `listen_prob_scale≤0.05` 时 `<|listen|>` logit 设 **-inf**(hard-listen,禁"只听不翻")
   - `is_end_token` 在 hard-listen 下忽略 `chunk_eos`,**连续生成**到 EOS

> 若迁移/重建后端,需重打这些 patch。备份在本地 `patches/patch_ws_duplex_translation.patch`(含全部 4 文件改动)。
> 应用方式:`cd /workspace/llama.cpp-omni && git apply /path/to/patch_ws_duplex_translation.patch`,然后重新编译(见 `docs/omni_inference_deployment.md` §4)。
